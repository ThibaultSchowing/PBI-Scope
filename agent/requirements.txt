# Pinned to langchain 1.x + LangGraph, which provide the modern tool-calling
# agent API (create_react_agent) and include the langchain-core >=1.2.22
# security fix for path traversal in legacy load_prompt functions (GHSA).
# The 0.3.x AgentExecutor-based implementation has been replaced; upgrade
# these pins together and re-test when moving to future major versions.
langchain==1.2.17
langchain-core==1.3.2
langchain-community==0.4.1
langchain-ollama==1.1.0
langgraph==1.1.10
fastapi>=0.109.0
uvicorn[standard]>=0.24.0
duckdb>=0.9.0
pandas>=2.0.0
numpy>=1.24.0
tabulate>=0.9.0
pydantic>=2.5.0
sse-starlette>=1.8.0

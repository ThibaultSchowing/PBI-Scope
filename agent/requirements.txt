# Pinned to the last 0.3.x / 0.2.x releases that still export AgentExecutor
# and create_tool_calling_agent from langchain.agents.  LangChain >=1.0 removed
# these symbols; unpinned >=0.3.0 constraints allowed 1.x to be installed and
# broke agent startup with an ImportError.  Upgrade only after migrating to the
# LangGraph-based agent API.
langchain==0.3.28
langchain-core==0.3.84
langchain-community==0.3.31
langchain-ollama==0.2.3
fastapi>=0.109.0
uvicorn[standard]>=0.24.0
duckdb>=0.9.0
pandas>=2.0.0
numpy>=1.24.0
pydantic>=2.5.0
sse-starlette>=1.8.0
